import React from 'react';
import Layout from "../../components/Layout";

const UserProfile = () => {
    return (
        <Layout>
            user profile
        </Layout>
    );
};

export default UserProfile